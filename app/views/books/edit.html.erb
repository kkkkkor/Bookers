<!--投稿編集画面-->
<h1>Editing Book</h1>
<%= form_with model: @books do |f| %>
<h4>title</h4>

<%= f.text_field :title %>
<h4>body</h4>
<%= f.text_area :body %>

<a><%= f.submit "up date Book"%></a>
<%= link_to "up date Book", book_path(@books.id) %>
<% end %>

<%= link_to 'Show', book_path %>|<%= link_to :back do %>
  Back
<% end %>